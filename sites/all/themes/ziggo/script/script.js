// $Id$

(function ($) {
  Drupal.behaviors.superLabels = {
    attach: function (context) {
       // Replace labels with 'superlabels' that are rendered on top of the inputs.
       // $("form", context).addClass("superlabels").superLabels();
    }
  };
})(jQuery);