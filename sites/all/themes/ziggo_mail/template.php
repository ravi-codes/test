<?php

/**
 * Process variables to format email messages.
 *
 * @see htmlmail.tpl.php
 */
function ziggo_mail_preprocess(&$variables, $hook) {
  //var_dump($hook);
  //var_dump($variables);
  if ($hook == 'page'){
    //var_dump($variables);
  }
}